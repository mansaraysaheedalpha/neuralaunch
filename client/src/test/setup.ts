// src/test/setup.ts
//
// Global vitest setup. Referenced by vitest.config.ts. Intentionally
// minimal — per-suite mocks (Prisma, getUserTier, etc.) are declared
// inside each test file so the test reads top-down.
export {};
