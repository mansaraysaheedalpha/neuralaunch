// Pushback round counts (10 / 15 / soft-warn at 4) shown by these visuals
// are a static mirror of PUSHBACK_CONFIG in
// packages/constants/src/discovery.ts. Verdict labels mirror PUSHBACK_ACTIONS
// in client/src/lib/discovery/pushback-engine.ts. If those constants change,
// update the mocks in ./recommendation-anatomy/ accordingly.

export { SampleRecommendationCard } from "./recommendation-anatomy/SampleRecommendationCard";
export { PushbackLadder } from "./recommendation-anatomy/PushbackLadder";
