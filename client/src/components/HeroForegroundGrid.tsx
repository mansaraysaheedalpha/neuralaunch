// src/components/HeroForegroundGrid.tsx
"use client";

import styles from "./HeroForegroundGrid.module.css";

export default function HeroForegroundGrid() {
  // This component just provides the structure for the CSS effect
  return (
    <div className={styles.gridContainer}>
      {/* We can use pseudo-elements or potentially map divs here if needed */}
    </div>
  );
}
